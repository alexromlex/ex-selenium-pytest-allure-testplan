# Маркер: security, slow
# Тест: Replay attack (повторное использование токена)
# Взять один валидный токен, отправить форму дважды с одним токеном
# Ожидаем: первый раз 200, второй раз 403 (токен уже использован)

# Тест: Массовая отправка с одним токеном (флуд)
# Ожидаем: rate limiting или блокировка